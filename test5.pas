begin
  integer k;
  integer function F(n);
    begin
      intger n;
      integer k;
      integer function A(x;
        begin
          integer x;
          A:=3
        end;
      n:=A(k);
      if n<=0 then F:=1
      else F:=n*F(n-1)
    end;
  integer k;
  read(m);
  k:=F(m);
  write(k)
